#include <iostream>
#include <vector>

//pass an option/flag to a function to set policy

enum action_t {DOUBLE, SQUARE };

void do_it ( std::vector<int>& v, action_t flag) {
	if (flag == TRIPLE ){
		v [i] *= 2;
	}
	else { 
		v[i] *= v[i];
	 }
}



int main () {



	return 0; 
}
